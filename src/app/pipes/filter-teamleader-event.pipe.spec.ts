import { FilterTeamleaderEventPipe } from './filter-teamleader-event.pipe';

describe('FilterTeamleaderEventPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeamleaderEventPipe();
    expect(pipe).toBeTruthy();
  });
});
