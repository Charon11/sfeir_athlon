import { EventsRankPipe } from './events-rank.pipe';

describe('EventsRankPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsRankPipe();
    expect(pipe).toBeTruthy();
  });
});
